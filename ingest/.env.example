# Only SQLx migration commands use DATABASE_URL; Python never connects to the DB.
DATABASE_URL=postgresql://exposed:exposed_local_dev@localhost:55432/exposed?sslmode=disable
PARLIAMENT_TERM_START=2024-07-04
EXPOSED_APP_URL=http://127.0.0.1:7000
# Compose development token; omit when using the loopback-only host dev config.
EXPOSED_IMPORT_TOKEN=exposed_local_import
